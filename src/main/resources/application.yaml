spring:
  application:
    name: gopizza
  datasource:
    url: jdbc:postgresql://localhost:5432/gopizza
    username: gopizza
    password: gopizza123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
